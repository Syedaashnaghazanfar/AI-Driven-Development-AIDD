# Simple CLI Calculator

This project contains a simple command-line interface (CLI) calculator written in Python. It supports basic arithmetic operations: addition, subtraction, multiplication, and division.

## Usage

To run the calculator, execute the `calculator.py` script:

```bash
python calculator.py
```

The calculator will prompt you to select an operation and enter two numbers. You can perform multiple calculations sequentially.