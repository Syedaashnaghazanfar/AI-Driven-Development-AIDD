# Project Overview: 01-FirstProject

This project appears to be a collection of small, independent Python-based command-line interface (CLI) applications or modules.

## Directory Structure

-   `calculator/`: Contains a simple Python CLI calculator application.
-   `cli-sessions/`: Contains a basic Python script for CLI sessions.

## Module Details

### `calculator/`

This module provides a command-line calculator with basic arithmetic operations.

-   **`calculator.py`**:
    -   Implements `add`, `subtract`, `multiply`, and `divide` functions.
    -   Features a `while` loop for continuous calculations until the user chooses to exit.
    -   Handles `ValueError` for invalid number inputs.
-   **`README.md`**:
    -   Describes the "Simple CLI Calculator".
    -   Provides instructions on how to run the `calculator.py` script.

### `cli-sessions/`

This module contains a minimal Python script.

-   **`main.py`**:
    -   Defines a `main()` function that prints "Hello from 01-firstproject!".
    -   Executes `main()` when the script is run directly.
-   **`README.md`**:
    -   Currently empty.

## Summary

The project currently consists of two distinct Python CLI components: a functional calculator and a minimal "hello world" type script. The `ashna` directory is present but empty.
